[project]
name = "tema2"
version = "0.1.0"
description = "Board game tracker REST API + web client built with Flask"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
	"flask>=3.1.0",
	"flask-sqlalchemy>=3.1.1",
]
